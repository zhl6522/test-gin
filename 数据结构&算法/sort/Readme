排序;
    冒泡排序、选择排序、插入排序、快速排序(依次速度越来越快)
             select   insert    quick

    快速排序法也不是同时排，仍然是按照递归方式走：
        每次处理都是两两交换，每次按照一半的方式来减少比较次数，所以说递减的速度会很快效率很高（减少了比较和交换的次数）；
        但是耗费资源也会比较大，因为它开了很多栈，每开一个栈都会吃掉一些资源（CPU）；